version: "3.9"

services:
  faceid:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eduscan-faceid
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
